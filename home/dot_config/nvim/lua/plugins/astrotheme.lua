---@type LazySpec
return {
  'AstroNvim/astrotheme',
  opts = {
    highlights = {
      astrodark = {
        ['SnacksPickerDir'] = { fg = '#696c76' }, -- `Comment`, but without `italic` to make it easier to read.
      },
    },
  },
}
